package org.tnsif.superkeyword;
//if we created object of child class then it will first call to parent class constructor then it will call(invoke) to child class constructor
public class SuperConstructorDemo {

	public static void main(String[] args) {
		//super keyword is always use before calling to child class constructor
		//if we do not use super keyword then it will also call to its parent constructor first
		//super()
		ButterScotch b=new ButterScotch();

	}

}
